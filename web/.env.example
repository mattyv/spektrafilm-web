# Adobe Developer Console → your project → Lightroom Services →
# OAuth Single-Page App credential. Public by design (it ships in the bundle),
# but keep the real .env out of git.
VITE_ADOBE_CLIENT_ID=
